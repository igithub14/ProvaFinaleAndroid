<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.example.cristiano.provafinaleandroid" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChoiceActivity"></activity>

        <activity
            android:name=".About"></activity>

        <activity
            android:name=".Portfolio"></activity>

        <activity
            android:name=".Utilities"></activity>

        <activity
            android:name=".Calcolatrice"></activity>

        <activity
            android:name=".LancioDadi"></activity>



    </application>

</manifest>
